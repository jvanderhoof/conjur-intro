{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/conjurdemos/conjur-intro/environments/schema.json",
  "title": "conjur-intro declarative environment",
  "description": "Describes the end state of a Conjur environment so it can be provisioned reproducibly. This is the contract for bin/env: an unsupported or misspelled key is a hard error rather than a silent no-op. Every constraint lives here rather than in bin/env, so a hand-written spec fails the same way a generated one does, and the accepted range of each field widens only as bin/env learns to build it -- a spec that validates is a spec that can be provisioned. See docs/declarative-environments.md.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version"],
  "properties": {
    "version": {
      "description": "Appliance image tag to provision, as passed to `bin/dap --version`. Must be a quoted string: unquoted `13.5` is a YAML number, and unquoted `13.10` would silently become 13.1. The pattern is docker's own tag grammar, and it matters because this value reaches a command line.",
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_][A-Za-z0-9._-]*$",
      "maxLength": 128
    },
    "leader": {
      "description": "The leader and its standby cluster.",
      "type": "object",
      "additionalProperties": false,
      "default": {},
      "properties": {
        "standbys": {
          "description": "Number of standbys to deploy alongside the leader. The ceiling is the compose topology's: docker-compose.yml defines conjur-master-1 through conjur-master-5, so a leader and at most 4 standbys. More would need new compose services, so it is refused here rather than failing partway through provisioning.",
          "type": "integer",
          "minimum": 0,
          "maximum": 4,
          "default": 0
        },
        "auto_failover": {
          "description": "Enrol the leader and its standbys in an auto-failover cluster named `production`. Requires at least 2 standbys, which the cross-field rule below enforces.",
          "type": "boolean",
          "default": false
        },
        "master_key_encryption": {
          "description": "Encrypt the leader's server keys with a master key (MKE), as `bin/dap --enable-mke` does. Done after the leader is configured and before any other node is seeded from it, because each seed has to carry the encrypted keys; standbys and followers are then configured with the same master key.",
          "type": "boolean",
          "default": false
        },
        "custom_certificates": {
          "description": "Replace the leader's self-signed certificates with third-party ones issued by the CA that `bin/generate-certs` creates, as `bin/dap --import-custom-certificates` does. Done before master key encryption, so the imported keys are encrypted along with the rest, and before any other node is seeded, so every seed carries them. The generated leader certificate names conjur-master-1 through conjur-master-3 only, which the cross-field rule below enforces.",
          "type": "boolean",
          "default": false
        },
        "generate_dh": {
          "description": "Have the leader generate its own Diffie-Hellman parameters rather than using the pre-generated ones in files/dhparam.pem, as `bin/dap --generate-dh` does. The appliance generates them in the background after it is configured, so provisioning waits for that to finish before seeding any other node from it.",
          "type": "boolean",
          "default": false
        }
      }
    },
    "followers": {
      "description": "Number of followers to deploy behind the follower load balancer, which is generated with one backend server per follower and balances across them round-robin. The ceiling is the compose topology's again: docker-compose.yml defines conjur-follower-1 through conjur-follower-3, so a fourth follower needs a new compose service, its host in the follower certificate and FOLLOWER_CEILING in bin/utils.sh raised with this maximum. More is refused here rather than provisioning three followers and reporting a mismatch for the rest.",
      "type": "integer",
      "minimum": 0,
      "maximum": 3,
      "default": 0
    },
    "sample_data": {
      "description": "Load the sample policy and variable values. Defaults to true: with no policy and no secrets there is nothing to reproduce a secret-retrieval issue against.",
      "type": "boolean",
      "default": true
    },
    "events": {
      "description": "RESERVED SEAM -- NOT IMPLEMENTED. A large share of real escalations are about transitions (an upgrade, a promotion, a triggered failover) rather than an end state, and bin/dap already models those as discrete actions. Reserving the key now means adding them later is an extension rather than a breaking redesign. Only an empty list validates today; see docs/declarative-environments.md.",
      "type": "array",
      "maxItems": 0
    }
  },
  "allOf": [
    {
      "title": "auto-failover needs a quorum",
      "description": "Auto-failover is an etcd cluster, and an etcd cluster elects a new leader by majority vote: with a leader and one standby, losing the leader leaves one node out of two, which is not a majority, so nothing is promoted. Three members is the smallest configuration that can fail over, which means at least 2 standbys. This is a cross-field rule rather than a check in bin/env so that a hand-written spec hits it exactly where a generated one does -- and hits it before the first standby is provisioned.",
      "if": {
        "type": "object",
        "required": ["leader"],
        "properties": {
          "leader": {
            "type": "object",
            "required": ["auto_failover"],
            "properties": {
              "auto_failover": { "const": true }
            }
          }
        }
      },
      "then": {
        "properties": {
          "leader": {
            "properties": {
              "standbys": { "minimum": 2 }
            }
          }
        }
      }
    },
    {
      "title": "custom certificates name at most 2 standbys",
      "description": "The custom leader certificate bin/generate-certs issues is defined by artifacts/certificate-generator/configuration/dap-master.json, whose hosts are conjur-master.mycompany.local and conjur-master-1 through conjur-master-3 -- the leader and 2 standbys. A third or fourth standby would serve a certificate that does not name it, which nothing connecting to it by name can verify. Widening this means adding hosts to that file, not changing bin/env.",
      "if": {
        "type": "object",
        "required": ["leader"],
        "properties": {
          "leader": {
            "type": "object",
            "required": ["custom_certificates"],
            "properties": {
              "custom_certificates": { "const": true }
            }
          }
        }
      },
      "then": {
        "properties": {
          "leader": {
            "properties": {
              "standbys": { "maximum": 2 }
            }
          }
        }
      }
    }
  ]
}
